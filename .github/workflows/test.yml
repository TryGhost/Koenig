name: Test
on:
  pull_request:
  push:
    branches:
      - main
      - 'renovate/*'
jobs:
  test:
    with:
      ref: ${{ github.sha }}
    name: Testing
    uses: tryghost/actions/.github/workflows/test.yml@main
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
