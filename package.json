{
  "private": true,
  "repository": "https://github.com/TryGhost/Koenig",
  "author": "Ghost Foundation",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @tryghost/koenig-lexical dev",
    "prepare": "husky .github/hooks",
    "presetup": "yarn",
    "setup": "yarn && lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "preship": "lerna run preversion && yarn test",
    "fix": "yarn cache clean && rm -rf node_modules && yarn",
    "ship": "lerna version --git-remote ${GHOST_UPSTREAM:-origin}",
    "ship:ci": "IS_SHIPPING=true lerna publish from-package --yes --no-private",
    "main": "git checkout main && git pull ${GHOST_UPSTREAM:-origin} main && yarn"
  },
  "devDependencies": {
    "@eslint/compat": "2.0.2",
    "@eslint/js": "9.39.3",
    "eslint": "9.39.3",
    "eslint-plugin-ghost": "3.4.4",
    "globals": "17.4.0",
    "typescript-eslint": "8.56.1",
    "husky": "9.1.7",
    "lerna": "9.0.5",
    "lint-staged": "15.5.2"
  }
}
